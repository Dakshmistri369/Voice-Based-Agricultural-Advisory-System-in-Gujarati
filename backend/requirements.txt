fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12
httpx==0.27.2
requests==2.32.3
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.5.2
aiofiles==23.2.1

# Supabase (optional — install separately if USE_SUPABASE=true)
# supabase==2.9.1
# psycopg2-binary==2.9.9   ← requires PostgreSQL client libs; skip for JSON-only mode

# Audio processing
pydub==0.25.1
soundfile==0.12.1

# numpy (pre-built Windows wheel — no C compiler needed)
numpy>=2.0.0
